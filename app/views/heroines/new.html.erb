<%= form_for @heroine do |h| %>
Name: <%= h.text_field :name %> <br>
Super Name: <%= h.text_field :super_name %> <br>
Power: <%= h.collection_select(:power_id, Power.all, :id, :name, :prompt => 'Select')%> <br>
<%= h.submit "submit" %>
<% end %>
